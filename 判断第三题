#include <stdio.h>
int main()
{
	int n;
	scanf_s("%d", &n);//输入到公司的距离
	float by,wk;//设置两种方式
	by = 50 + (n / 3);
	wk = n / 1.2;
	if (by == wk)
		printf("一样快\n");
	else if (by > wk)
		printf("走路\n");
	else printf("骑车\n");
	return 0;
}

#include <stdio.h>
int main()
{
	int a, b, c;
	scanf_s("%d%d%d", &a, &b, &c);//输入三个数
	int max=a;//先给max赋值
	if (max < b)与b比较，谁大谁为max
		max = b;
	if (max < c)与c比较，谁大为max
		max = c;
	printf("最大值：%d\n", max);
}
